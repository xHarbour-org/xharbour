#
# $Id: global.cf,v 1.2 2003/09/08 12:56:52 druzus Exp $
#

GRANDP = ../../
ARCH := $(HB_ARCHITECTURE)/$(HB_COMPILER)

HB_DB_DRIVERS=\
	nulsys \
	dbfntx \
	dbfcdx \
	dbffpt \
	dbfdbt \
	dbfcdxold \

ifeq ($(HB_BIN_COMPILE),)
HB_BIN_COMPILE := $(TOP)$(ROOT)source/compiler/$(ARCH)
endif

ifeq ($(HB_INC_COMPILE),)
HB_INC_COMPILE := $(TOP)$(ROOT)include
endif

ifeq ($(HB_LIB_COMPILE),)
HB_LIB_COMPILE :=
endif

include $(TOP)$(ROOT)config/$(ARCH).cf
