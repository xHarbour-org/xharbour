
!include $(WH_DIR)\env\makeb32.env

lib: $(WHAT32LIB) 

$(WHAT32LIB): $(WHAT32OBJ)
   -erase $(WHAT32LIB)
   $(lib) "$(WHAT32LIB)" /P512 @&&!
+$(**: = &^
+)

!

clean:
        -erase *.obj
        -erase *.exe
        -erase *.res
        -erase *.map
        -erase *.rws
        -erase $(WHAT32LIB)


