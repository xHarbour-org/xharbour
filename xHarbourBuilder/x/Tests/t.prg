Procedure Main()

// Comment added to test GIT functionality in Xcode

   LOCAL Var := 1;Var++

   Var;
   := 1

   IF .T.
      text QOut,QQOut
         bla bla
         ga ga
      ENDTEXT
      
      7++
      
      IF .F.
         Var += 5; Var1++
      ELSE
         Var -= 5
      ENDIF
   ELSEIF .F.
      Var := 0
   ELSE
      --Var
   ENDIF
   
   b := {|_1| qout(_1)++ }
   
   ++Var
   
   Var1 := Var2 + 1
   
   Var3 := Test()[1;
   ] := "no mor;"
   QOut( Var4 ) 

   M->Var := 2 + 1 * 5 - 4
   
   MEMVA->Var := ( 2 * 3 )
      
RETURN

Function Test

RETURN { 1, NIL, 3 }
